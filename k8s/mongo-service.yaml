apiVersion: v1
kind: Service
metadata:
  name: mongo
  labels:
    app: mongo
    tier: database
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
  ClusterIP: None
  selector:
    app: mongo
    tier: database